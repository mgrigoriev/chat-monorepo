apiVersion: v1
kind: Service
metadata:
  name: servers
  namespace: main
spec:
  type: LoadBalancer
  selector:
    app: servers
  ports:
    - name: http
      protocol: TCP
      targetPort: 8080
      port: 82
