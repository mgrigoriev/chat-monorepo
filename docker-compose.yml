services:
  chatmessages:
    build: chatmessages/
    command: ./main
    ports:
      - "8081:8080"  # REST API
      - "9091:9090"  # gRPC
      - "8888:8888"  # Swagger UI
  chatservers:
    build: chatservers/
    command: ./main
    ports:
      - "8082:8080"
  users:
    build: users/
    command: ./main
    ports:
      - "8083:8080"
